---
title: TDD-First Enforcement
scope: global
category: code
icon: &#128308;
description: Mandatory test-first development for all non-trivial code. No trigger words needed — always active. RED-GREEN-REFACTOR cycle enforced for TypeScript, Python, Node.js, and Go.
triggers:
  - always active — no trigger needed
  - write tests first
  - TDD
  - test first
checks-label: Rules
checks:
  - Always active — no trigger words needed
  - Tests written BEFORE implementation (happy + sad + edge)
  - RED confirmed before code, GREEN confirmed after
  - Standardised test file placement per runtime
version: 2.0
---


